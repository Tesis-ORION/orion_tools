
# Occupancy grid params for the cartographer ros related executions

occupancy_grid_node:
  ros__parameters:
    use_sim_time: false     # To specify usage of simulated time
    resolution: 0.05        # Grid resolution in meters
    publish_period_sec: 1.0 # Period of publication of the grid map
    frame_id: map           # Tf that contains the map
